// Illustrations for "general" slides are intentionally omitted at runtime to avoid
// broken external image requests; key-points slides carry the content instead.
export async function image_query() {
  return null;
}
